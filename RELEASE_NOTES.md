# Traffic Monitor v2.5.0

## 新功能

- 新增「过滤 DIRECT 直连流量」开关（页面右上角「设置」内）：开启后，链路上出现 `DIRECT` 的连接在采集阶段就被跳过，不再写入统计。趋势、总量，以及设备 / 主机 / 代理 / 规则各维度都不再包含直连流量，自动切换评估也不再被直连流量触发。
- 开关默认关闭，状态持久化在本地 SQLite，重启后仍然生效；已有的历史数据不受影响。

## 行为说明

- 过滤期间每条连接的采集基线仍会推进，关闭开关后从当前时刻继续记录，不会把过滤期间的流量补记成一次尖峰。
- 开启开关时，已经落入当前 1 分钟聚合桶的直连数据最多残留一个桶，不会回滚。
